# WSL 踩坑笔记

## 1. “装完了但不能用”：很多时候只是没重启
现象：`wsl --status` 仍提示“不支持 WSL2”，或 `wsl -l -v` 显示没有发行版。

优先做两件事：
- 重启 Windows
- 再跑：`wsl --status`、`wsl -l -v`

更完整排错：`WSL2-安装.md`

## 2. `/mnt/c` / `/mnt/d` 运行很慢
- Windows 盘可以读写，但遇到“依赖安装/构建/大量小文件”时经常变慢。
- 思路：把仓库放到 Linux 路径（如 `~/projects`），在 WSL 里运行。
