1. ```
   谷歌浏览器真机调试（只适用于安卓）
   1.谷歌浏览器输入：chrome://inspect/#devices；勾选：Discover USB devices
   2.手机与电脑usb连接；开启开发者权限；开启usb调试；会有调试弹框弹出，勾选同意；
   3.可以下载工具用于检测手机与电脑是否连接：https://developer.android.com/tools/releases/platform-tools?hl=zh-cn#downloads:~:text=Download%20Android%20SDK%20Platform%2DTools%20for%20Windows；下载完成后打开该目录终端；输入命令 .\abd.exe devices;设备状态为device标识连接正常;
   ```


2. vscode内网穿透：https://zhuanlan.zhihu.com/p/686951133；
2. 使用Vue Dev工具调试线上vue2项目：https://juejin.cn/post/7324643000700502031；

   ```js
   var Vue, walker, node;
   walker = document.createTreeWalker(document.body,1);
   while ((node = walker.nextNode())) {
     if (node.__vue__) {
       Vue = node.__vue__.$options._base;
       if (!Vue.config.devtools) {
         Vue.config.devtools = true;
         if (window.__VUE_DEVTOOLS_GLOBAL_HOOK__) {
           window.__VUE_DEVTOOLS_GLOBAL_HOOK__.emit("init", Vue);
           console.log("==> vue devtools now is enabled");
         }
       }
       break;
     }
   }
   ```

4. windows调试IOS H5

   ```
   npm install remotedebug-ios-webkit-adapter -g
   iPhone或iPad通过数据线连接到电脑上，打开iTunes或爱思助手，并在手机上信任此电脑。
   运行 remotedebug_ios_webkit_adapter --port=9000
   打开谷歌/Edge开发者工具 chrome://inspect/#devices 或 edge://inspect/#devices
   https://juejin.cn/post/7307515606006087720 看4.3 4.4
   ```

   
